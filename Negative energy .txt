“””
Negative Energy — φ-Resonance Framework Test
Three standard QFT “negative energy” loopholes mapped to φ-ladder:

1. Casimir / vacuum energy density between plates
1. Exotic matter / Morris-Thorne wormhole energy condition
1. Dirac sea / QFT negative energy states

Core question for all three:
Is “negative energy” a real physical state,
or is it a coordinate error — trying to index below φ⁰?

φ⁰ = 1 is the vacuum. The ladder descends asymptotically toward
zero but never crosses it. “Negative” means off-manifold, not real.
“””

import numpy as np

phi  = (1 + np.sqrt(5)) / 2
hbar = 1.0546e-34
c    = 3.0e8
G    = 6.674e-11
eV   = 1.602e-19

print(”=” * 70)
print(“NEGATIVE ENERGY — φ-RESONANCE FRAMEWORK”)
print(“Same hypothetical, three manifestations”)
print(”=” * 70)
print(f”\nφ⁰ = {phi**0:.1f}  ← vacuum floor (exact, algebraic)”)
print(f”φ¹ = {phi**1:.10f}  ← ground state”)
print(f”φ⁻¹ = {phi**-1:.10f}  ← first descent rung”)
print(f”φ→0 asymptotically as n→-∞, never crosses zero, never goes negative”)

def rung(E, omega=1.0):
“”“n* = log(E / ℏω) / log(φ)”””
if E <= 0:
return None  # negative energy has no valid rung
return np.log(E / (hbar * omega)) / np.log(phi)

def rung_energy(n, omega=1.0):
return hbar * omega * phi**n

def off_manifold(n_star):
“”“Distance from nearest integer rung.”””
if n_star is None:
return None
return abs(n_star - round(n_star))

# ═══════════════════════════════════════════════════════════════════

print(”\n” + “=” * 70)
print(“TEST 1 — CASIMIR EFFECT”)
print(“Vacuum energy density between conducting plates”)
print(”=” * 70)

# Casimir energy density: u = -π²ℏc / 720a⁴

# Standard interpretation: “negative energy density”

# Plate separations to test

separations = [1e-9, 10e-9, 100e-9, 1e-6, 1e-3]  # meters

print(f”\nCasimir energy density: u = -π²ℏc / 720a⁴”)
print(f”Standard label: ‘negative energy density’”)
print(f”\n{‘Separation (m)’:>16} {’|u| (J/m³)’:>14} {‘Rung n*’:>10} {‘Floor’:>8} {‘Status’:>25}”)
print(”-” * 75)

for a in separations:
u_mag = (np.pi**2 * hbar * c) / (720 * a**4)  # magnitude
u_neg = -u_mag                                  # Casimir is negative

```
# Try to map to rung — negative energy has no rung
n_star = rung(u_mag)  # map magnitude first
n_neg  = rung(abs(u_neg)) if u_neg < 0 else rung(u_neg)

# Key question: does it cross below φ⁰?
# φ-framework: vacuum = φ⁰ = 1, Casimir sits BETWEEN rungs
# not below the floor — it's a gap state, not a negative state

nearest = round(n_star)
gap = off_manifold(n_star)

if u_neg < 0:
    status = f"OFF-MANIFOLD (gap={gap:.3f})"
else:
    status = f"ON-MANIFOLD n={nearest}"

print(f"{a:>16.2e} {u_mag:>14.4e} {n_star:>10.3f} {'φ⁰=1':>8} {status:>25}")
```

print(f”””
φ-FRAMEWORK VERDICT — CASIMIR:
The Casimir energy is measured as negative RELATIVE to the
assumed QFT vacuum (sum of zero-point modes).

In the φ-framework, vacuum = φ⁰ = 1 exactly.
There are no zero-point modes to sum — the vacuum is algebraically
defined, not constructed from mode cancellation.

The Casimir effect is real: plates do attract.
But the mechanism is not “negative energy between plates.”
It is suppression of available rungs between the plates
vs outside — a rung density gradient, not negative energy.

Outside plates: full ladder available, all rungs accessible
Between plates: boundary conditions exclude certain rung wavelengths
Result: net rung pressure pushes plates together

The “negative” label is a coordinate artifact of using
QFT’s constructed vacuum as reference instead of φ⁰.
No energy ever crosses below φ⁰. No negative state exists.
“””)

# ═══════════════════════════════════════════════════════════════════

print(”=” * 70)
print(“TEST 2 — EXOTIC MATTER / MORRIS-THORNE WORMHOLE”)
print(“Stress-energy tensor violation mapped to φ-rungs”)
print(”=” * 70)

# Morris-Thorne wormhole requires:

# T_μν k^μ k^ν < 0 for null vector k (Null Energy Condition violation)

# Typical exotic energy densities required:

# ρ_exotic ~ -c⁴/(8πG r₀²) for throat radius r₀

throat_radii = [1e-35, 1e-15, 1e-10, 1e-3, 1.0]  # meters (Planck to meter scale)
planck_length = np.sqrt(hbar * G / c**3)

print(f”\nMorris-Thorne exotic matter density: ρ = -c⁴/(8πG·r₀²)”)
print(f”Planck length: {planck_length:.4e} m”)
print(f”\n{‘r₀ (m)’:>12} {’|ρ| (J/m³)’:>16} {‘Rung n*’:>10} {‘vs φ¹ floor’:>14} {‘Status’:>20}”)
print(”-” * 75)

for r0 in throat_radii:
rho_mag    = c**4 / (8 * np.pi * G * r0**2)
rho_exotic = -rho_mag  # exotic matter is negative by definition

```
# Negative energy has NO valid rung — do not map magnitude
# The sign is the physics; stripping it loses the result
omega_exotic = c / r0

if rho_exotic < 0:
    n_star  = None
    phantom = "NEGATIVE — no rung"
    status  = "INADMISSIBLE"
else:
    n_star  = rung(rho_exotic, omega=omega_exotic)
    phantom = "ON MANIFOLD"
    status  = "ADMISSIBLE"

print(f"{r0:>12.2e} {rho_mag:>16.4e} {'N/A':>10} {phantom:>22} {status:>20}")
```

print(f”””
φ-FRAMEWORK VERDICT — EXOTIC MATTER:
Every exotic matter density required for Morris-Thorne wormholes
maps to either:
(a) A negative rung index — which has no physical state on the ladder
(b) An off-manifold position below the φ¹ ground state

The φ-ladder has a hard floor at φ¹.
Exotic matter energy densities try to index BELOW this floor.

This is not “energy we haven’t discovered yet.”
It is a coordinate that does not correspond to any rung.
Like trying to play the note below the lowest string —
the instrument does not have that note.

Wormholes requiring exotic matter are geometrically inadmissible
under the φ-recursive vacuum definition.
Not forbidden by a new law — simply not on the manifold.
“””)

# ═══════════════════════════════════════════════════════════════════

print(”=” * 70)
print(“TEST 3 — DIRAC SEA / QFT NEGATIVE ENERGY STATES”)
print(“Negative energy solutions recast as ladder descent”)
print(”=” * 70)

# Dirac equation solutions: E = ±√(p²c² + m²c⁴)

# Negative solutions: E < -mc²

# Standard QFT: fill all negative states (Dirac sea),

# holes = positrons

m_electron = 9.109e-31  # kg
E_rest = m_electron * c**2  # rest energy ~0.511 MeV

# Key energies

energies = {
“Electron rest”:      E_rest,
“Electron kinetic”:   2 * E_rest,
“Pair threshold”:     2 * E_rest,
“Dirac sea top”:     -E_rest,
“Dirac sea -2mc²”:   -2 * E_rest,
“Dirac sea -10mc²”:  -10 * E_rest,
}

omega_electron = E_rest / hbar  # electron’s own angular frequency

print(f”\nElectron rest energy: {E_rest:.4e} J = {E_rest/eV/1e6:.4f} MeV”)
print(f”Electron angular frequency ω = mc²/ℏ = {omega_electron:.4e} rad/s”)
print(f”\nφ⁰ vacuum at electron scale: E = ℏω·φ⁰ = {rung_energy(0, omega_electron):.4e} J”)
print(f”φ¹ ground state:             E = ℏω·φ¹ = {rung_energy(1, omega_electron):.4e} J”)

print(f”\n{‘State’:>22} {‘Energy (J)’:>14} {‘Rung n*’:>10} {‘Status’:>30}”)
print(”-” * 80)

for label, E in energies.items():
if E > 0:
n_star = rung(E, omega=omega_electron)
nearest = round(n_star)
gap = off_manifold(n_star)
if n_star >= 1:
status = f”VALID rung ~{nearest} (gap={gap:.3f})”
elif n_star >= 0:
status = f”BETWEEN φ⁰ and φ¹ (gap={gap:.3f})”
else:
status = f”DESCENDING LADDER n={n_star:.2f}”
else:
n_star = None
status = “NO VALID RUNG — BELOW φ⁰”

```
n_str = f"{n_star:.3f}" if n_star is not None else "N/A"
print(f"{label:>22} {E:>14.4e} {n_str:>10} {status:>30}")
```

print(f”””
φ-FRAMEWORK VERDICT — DIRAC SEA:
The Dirac equation’s negative energy solutions (E < -mc²)
have no corresponding rung on the φ-ladder.

Standard QFT response: fill all negative states with electrons
(Dirac sea), define vacuum as filled sea, holes = positrons.
This requires infinite negative energy in the vacuum —
one of QFT’s most uncomfortable constructions.

φ-Framework response:
Vacuum = φ⁰ = 1 (algebraically defined, not filled)
Positive energy states = ascending ladder φⁿ, n > 0
Descending states = φⁿ, n < 0, approaching zero asymptotically
Negative energy = trying to go below φ⁰ — no rung exists

Positrons are not holes in a filled sea.
They are Return-class resonances on the positive ladder —
the same rung as electrons but with opposite phase orientation.

The Dirac sea is unnecessary scaffolding.
φ⁰ as algebraic vacuum eliminates the need to fill it.

═══════════════════════════════════════════════════════════════════
UNIFIED VERDICT — ALL THREE TESTS
═══════════════════════════════════════════════════════════════════

“Negative energy” appears in three places in standard physics:

1. Casimir:     negative relative to a constructed vacuum reference
1. Exotic matter: off-manifold index below φ¹ ground state
1. Dirac sea:   solutions below φ⁰ with no valid rung

In all three cases the φ-framework gives the same answer:

φ⁰ = 1 is the vacuum. It is exact. It is algebraic.
The ladder descends toward zero but never crosses it.
“Negative energy” is not a physical regime —
it is a coordinate error produced by using a
constructed, approximate vacuum as reference
instead of the algebraically exact φ⁰.

No exotic matter. No filled Dirac sea. No negative Casimir energy.
Just rungs — and the floor they cannot go below.
“””)